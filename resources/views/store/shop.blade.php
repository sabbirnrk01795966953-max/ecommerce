@extends('layouts.store')
@section('title','শপ - '.($siteSettings['site_name']??'Trendy Deal BD'))
@section('content')<div class="breadcrumb"><div class="container"><a href="{{ route('home') }}">হোম</a> <span>›</span> শপ</div></div><div class="container shop-layout"><aside class="category-sidebar"><h3>ক্যাটাগরি</h3>@foreach($categories as $cat)<b>{{ $cat->name_bn }}</b>@foreach($cat->subcategories as $sub)<a href="{{ route('subcategory.show',$sub->slug) }}">{{ $sub->name_bn }}</a>@endforeach @endforeach</aside><section><div class="section-row"><div>{{ $products->total() }} টি পণ্য</div></div><div class="product-grid">@foreach($products as $product)@include('store.partials.product-card',['product'=>$product])@endforeach</div>{{ $products->links('vendor.pagination.default') }}</section></div>@endsection
