@extends('layouts.admin')@section('title','সাবক্যাটাগরি')@section('page_title','সাবক্যাটাগরি')@section('content')<div class="page-actions"><a class="btn primary" href="{{ route('admin.subcategories.create') }}">+ নতুন সাবক্যাটাগরি</a></div><div class="panel table-wrap"><table><thead><tr><th>নাম</th><th>ক্যাটাগরি</th><th>Last URL</th><th>Link</th><th>স্ট্যাটাস</th><th></th></tr></thead><tbody>@foreach($subcategories as $s)<tr><td>{{ $s->name_bn }}</td><td>{{ $s->category?->name_bn }}</td><td><code>{{ $s->slug }}</code></td><td><a target="_blank" href="{{ route('subcategory.show',$s->slug) }}">Open ↗</a></td><td>{{ $s->is_active?'Active':'Inactive' }}</td><td class="actions"><a href="{{ route('admin.subcategories.edit',$s) }}">Edit</a><form method="post" action="{{ route('admin.subcategories.destroy',$s) }}" onsubmit="return confirm('Delete?')">@csrf @method('DELETE')<button>Delete</button></form></td></tr>@endforeach</tbody></table></div>{{ $subcategories->links('vendor.pagination.default') }}@endsection
