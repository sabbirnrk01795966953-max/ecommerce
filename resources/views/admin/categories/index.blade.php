@extends('layouts.admin')@section('title','ক্যাটাগরি')@section('page_title','ক্যাটাগরি')@section('content')<div class="page-actions"><a class="btn primary" href="{{ route('admin.categories.create') }}">+ নতুন ক্যাটাগরি</a></div><div class="panel table-wrap"><table><thead><tr><th>নাম</th><th>Last URL</th><th>স্ট্যাটাস</th><th></th></tr></thead><tbody>@foreach($categories as $c)<tr><td>{{ $c->name_bn }}</td><td><code>{{ $c->slug }}</code></td><td>{{ $c->is_active?'Active':'Inactive' }}</td><td class="actions"><a href="{{ route('admin.categories.edit',$c) }}">Edit</a><form method="post" action="{{ route('admin.categories.destroy',$c) }}" onsubmit="return confirm('Delete?')">@csrf @method('DELETE')<button>Delete</button></form></td></tr>@endforeach</tbody></table></div>{{ $categories->links('vendor.pagination.default') }}@endsection
